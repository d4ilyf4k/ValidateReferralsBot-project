# **Техническое задание: Telegram-бот «Рефералы Банков»**

## **1. Общее описание**  
Бот автоматизирует сбор данных от рефералов, отслеживает выполнение условий по программам приветственных бонусов **Т-Банка и Альфа-Банка**, рассчитывает вознаграждения для реферера и реферала, а также предоставляет детальную финансовую отчётность в **JSON** и человекочитаемом виде. Все взаимодействия — через интуитивные **инлайн- и reply-кнопки**. Регистрация — пошаговая с возможностью обновления данных на любом этапе. Поддерживается **поиск, напоминания и полный экспорт данных**. Администратор (вы) получает сводную аналитику и управление UTM-ссылками. Рефералы из любых регионов РФ (и не только) поддерживаются. Профиль можно редактировать по полям без повторной полной регистрации.

---

## **2. Функциональные требования**

### **2.1. Регистрация реферала (`/start`)**
- **Приветствие**:  
  При первом запуске — краткое описание бота и кнопка **«Начать регистрацию»**.  
  Для зарегистрированных — отображение краткого профиля и кнопок:  
  - 📊 **Статус заявки**,  
  - 💰 **Финансовый отчёт**,  
  - ✏️ **Редактировать профиль**,  
  - 🗑 **Очистить историю**.
- **Пошаговый диалог** (через `FSM` со states):  
  Каждый шаг включает:  
  - Текст вопроса и текущее значение (если уже введено).  
  - Reply-клавиатуру с действиями: ◀️ **Назад**, ▶️ **Далее**.
  **Последовательность шагов**:  
  1. **ФИО** (обязательно): текст, валидация на формат (минимум 2 слова, кириллица).  
  2. **Телефон** (обязательно): кнопка **«📱 Отправить номер»** (`request_contact=True`).  
  3. **Банк** (обязательно): 🏦 **Т-Банк** / 🏦 **Альфа-Банк** (reply-кнопки).  
  4. **Подтверждение**:  
     - Сводка профиля + ✅ **Сохранить** / 🔁 **Изменить**.

> 🔐 Все персональные данные (включая телефон) **шифруются** при сохранении в БД с использованием `Fernet`.

---

### **2.2. Главное меню (для зарегистрированных рефералов)**
После `/start` отображается меню с **reply-кнопками**.

#### **2.2.1. ℹ️ «Помощь»**
- Отображает краткую справку по возможностям бота и основным командам.
- Содержит пошаговое описание процесса получения бонуса и условия для Т-Банка и Альфа-Банка.
- Предоставляет контакты поддержки (вашего друга, который пригласил реферала).

#### **2.2.2. 📊 «Статус заявки»**
- Отображает текущий статус по этапам (`card_activated`, `purchase_made`).  
- Показывает сумму бонуса и его статус (`ожидает`, `подтверждено`).  
- Обновляется автоматически при любом изменении профиля.

#### **2.2.3. 💰 «Финансовый отчёт»**
- Отображает **полный отчёт** с условиями бонуса и статусом выплаты.  
- Поддерживает команду **`/status`** (альтернатива кнопке).

#### **2.2.4. ✏️ «Редактировать профиль»**
- Позволяет обновить: ФИО, номер, банк, статус активации, покупки.  
- После ввода — данные сохраняются, пересчитываются бонусы.

#### **2.2.5. 🏦 «Выбрать банк»**
- Отправляет **персональную UTM-ссылку** и **подробные условия бонуса** для выбранного банка.

#### **2.2.6. 🗑 «Очистить историю»**
- Полностью удаляет все данные пользователя из БД (соответствие ФЗ-152).  
- Возвращает в состояние «нового пользователя».

---

### **2.3. Расчёт вознаграждений**
- **Правила выплат** задаются в коде:
  ```python
  payment_rules = {
      "t-bank": {"bonus": 500, "conditions": {"card_activated": True, "purchase_made": True}},
      "alpha": {"bonus": 700, "conditions": {"card_activated": True, "purchase_made": False}}
	  }```
- При обновлении любого поля, влияющего на условия (`card_activated`, `purchase_made`), система:
    
    1. Сравнивает фактические данные с условиями,
    2. Если все условия выполнены — устанавливает `your_bonus_status = "confirmed"`,
    3. Сохраняет сумму и дату (дата = текущая).
    
- **Расчёт выполняется автоматически при любом обновлении профиля**.

---

### **2.4. Учёт данных реферала**

- Каждый этап сохраняется в БД с датой и флагом.
- Пользователь может обновлять данные в любой момент.
- Все изменения логируются (опционально — для аудита).

---

### **2.5. Уведомления и напоминания**

#### **Общая логика**

- Напоминания отправляются **только по запросу администратора** (вручную через `/remind`).
- Все уведомления привязаны к данным пользователя.

#### **Ручной триггер**

- Команда `/remind +79161234567`:
    - Находит реферала,
    - Отправляет сообщение:
        
        > «👋 Привет! Пожалуйста, обновите статус вашей заявки — это поможет нам быстрее начислить бонус!»
        
    - Логирует факт отправки.

---

### **2.6. Управление через команды**

- `/start` — главное меню (для зарегистрированных) или регистрация (для новых).
- `/status` — отобразить текущий статус заявки.
- `/help` — справка по возможностям бота.
- **Админ-команды (только для вас по `user_id`)**:
    - `/report` — полный отчёт по всем рефералам в **JSON**.
    - `/finance_total` — сводный финансовый отчёт.
    - `/finance_referral +79161234567` — отчёт по конкретному рефералу.
    - `/find +79161234567` — поиск по зашифрованному номеру.
    - `/remind +79161234567` — отправить напоминание.
    - `/update_link t-bank https://... utm_source=...` — обновить UTM-ссылку. 

---

## **3. Структура базы данных (SQLite)**

> 💡 Используется **SQLite** для простоты развёртывания (файл в корне проекта). Легко заменить на PostgreSQL.

### **Таблица `users`**

| Поле         | Тип          | Обязательность |
| ------------ | ------------ | -------------- |
| `user_id`    | INTEGER (PK) | Да             |
| `full_name`  | TEXT         | Да             |
| `phone_enc`  | BLOB         | Да (шифр.)     |
| `bank`       | TEXT         | Да             |
| `created_at` | DATETIME     | Да             |
### **Таблица `referral_progress`**

| Поле                  | Тип          |
| --------------------- | ------------ |
| `user_id`             | INTEGER (PK) |
| `card_activated`      | INTEGER      |
| `card_activated_date` | TEXT         |
| `purchase_made`       | INTEGER      |
| `first_purchase_date` | TEXT         |
### **Таблица `financial_data`**

|Поле|Тип|
|---|---|
|`user_id`|INTEGER (PK)|
|`your_bonus_amount`|INTEGER|
|`your_bonus_status`|TEXT|
### **Таблица `referral_links`**

| Поле           | Тип       |
| -------------- | --------- |
| `bank`         | TEXT (PK) |
| `url`          | TEXT      |
| `utm_source`   | TEXT      |
| `utm_medium`   | TEXT      |
| `utm_campaign` | TEXT      |
### **Таблица `reminders_log`**

|Поле|Тип|
|---|---|
|`id`|INTEGER (PK)|
|`user_id`|INTEGER|
|`admin_id`|INTEGER|
|`sent_at`|DATETIME|

---

## **4. Технические требования**

- **Язык интерфейса**: Русский.
- **Фреймворк**: `aiogram 3.x` (асинхронный режим).
- **База данных**: **SQLite** (файл в корне проекта — лёгкий запуск без настройки СУБД).
- **Безопасность**:
    
    - Шифрование телефона (`cryptography.fernet`),
    - Параметризованные запросы,
    - API-ключи и `BOT_TOKEN` — в `.env`.
    
- **Надёжность**:
    - Резервное копирование БД (просто архивируйте `referral_bot.db`),
    - Логирование критических действий (админ-команды, экспорт).

---

## **5. Ограничения**

- Реферальный код — **не используется** (управление через UTM-ссылки).
- Ручной ввод дат — в формате `ДД.ММ.ГГГГ` с валидацией.
- Админ-команды доступны **только по вашему `user_id`**.
- Автоматические напоминания — **не реализованы** (только ручной триггер через `/remind`).

---

## **6. Дальнейшее развитие (опционально)**

- Экспорт отчётов в Google Sheets.
- Веб-панель для аналитики.
- Интеграция с партнёрскими API банков (для автоматической проверки статуса).

---

### 🌲 **Структура проекта (в виде UNIX-дерева)**

```bash
referral_bot/

├── .env                    # BOT_TOKEN, DATABASE_URL, ADMIN_IDS, ENCRYPTION_KEY
├── .gitignore
├── README.md
├── requirements.txt
├── main.py                 # Точка входа (main)
│
├── config/
│ ├── __init__.py
│ └── settings.py           # Загрузка и валидация настроек из .env
│
├── core/
│ ├── __init__.py
│ ├── bot_instance.py       # Создание Application, подключение роутеров и JobQueue
│ └── logger.py             # Настройка логгера
│
├── database/
│ ├── __init__.py
│ ├── models.py                # Описание моделей (опционально)
│ └── db_manager.py            # Класс/функции для работы с SQLite
│
├── services/
│ ├── __init__.py
│ ├── bonus_calculator.py      # Расчёт вознаграждений по правилам
│ └── report_generator.py      # Генерация JSON и текстовых отчётов
│
├── handlers/
│ ├── __init__.py
│ ├── start_handler.py         # /start + меню
│ ├── onboarding_handler.py    # Регистрация (FSM)
│ ├── profile_handler.py       # Редактирование профиля
│ ├── bank_handler.py          # Выбор банка
│ ├── status_handler.py        # Статус заявки
│ ├── finance_handler.py       # Финансовые отчёты
│ ├── admin_handler.py         # Админ-команды
│ ├── user_handler.py          # Команды рефералов (/help, /status, очистка)
│ └── callback_handlers.py     # Инлайн-кнопки админа
│
├── utils/
│ ├── __init__.py
│ ├── validation.py            # Валидация ФИО, дат, телефонов
│ └── keyboards.py             # Фабрика клавиатур (инлайн и reply)
│
├── jobs/
│ ├── __init__.py
│ └── reminder_job.py          # Фоновые напоминания (опционально)
```

---

## 🔍 **Подробное описание компонентов**
### **1. Корень проекта**

- **`main.py`**: точка входа, запуск polling.
- **`.env`**:

  ```env

  BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

  DATABASE_URL=sqlite:///referral_bot.db

  ADMIN_IDS=123456789

  ENCRYPTION_KEY=your_fernet_key_here```
    
- **`requirements.txt`**: `aiogram`, `aiosqlite`, `python-dotenv`, `cryptography`, `apscheduler`.
- **`requirements.txt`**: `aiogram`, `aiosqlite`, `python-dotenv`, `cryptography`, `apscheduler`.
---

### **2. `config/` — Настройки**

- **`settings.py`**: загрузка переменных из `.env` с валидацией (включая проверку `ENCRYPTION_KEY`).

---

### **3. `core/` — Ядро бота**

- **`bot_instance.py`**:
    - Создаёт `Dispatcher`,
    - Подключает все роутеры,
    - Настраивает `APScheduler` для фоновых задач.

---

### **4. `database/` — Работа с данными**

- **`db_manager.py`**:
    - Все CRUD-операции,
    - Шифрование/дешифрование телефона,
    - Агрегация данных для отчётов.

> ✅ Преимущество: при переходе на PostgreSQL — меняется **только эта папка**.

---

### **5. `services/` — Бизнес-логика**

Здесь **вся «умная» логика**, независимая от Telegram.

- **`bonus_calculator.py`**:
   ```python
   def recalculate_bonus(user_id: int) -> None:
    # сверяет условия и обновляет статус бонуса
   ```

- **`report_generator.py`**:
   ```python
   def generate_referral_json(user_ dict) -> str:
    # возвращает JSON по шаблону из ТЗ
   ```
> ✅ Это позволяет **тестировать логику без запуска бота**.

---

### **6. `handlers/` — Взаимодействие с пользователем**

Каждый файл — **одна функциональная зона**.

- **`onboarding_handler.py`**: полный `FSM` для регистрации.
- **`profile_handler.py`**: мини-диалоги для редактирования.
- **`admin_handler.py`**: обработка `/find`, `/remind`, `/report`.
- **`callback_handlers.py`**: общие обработчики кнопок.

---

### **7. `utils/` — Вспомогательные утилиты**

- **`validation.py`**:
    - `is_valid_full_name(text)`,
    - `is_valid_date(date_str)`,
    - `is_valid_card_last4(text)`.

- **`keyboards.py`**:
    - `get_bank_choice_kb()`,
    - `get_main_menu_kb()`,
    - `get_phone_request_kb()`.

---

### **8. `jobs/` — Фоновые задачи**

- **`reminder_job.py`**:
```python
async def send_reminders_job(context):
    # сканирует пользователей, которым нужно напомнить
	# вызывает send_reminder_to_user
```

---

## 🧩 **Преимущества такой структуры**

| Принцип                        | Реализация                                                  |
| ------------------------------ | ----------------------------------------------------------- |
| **Разделение ответственности** | View (handlers), Data (database), Logic (services).         |
| **Тестируемость**              | `services/` можно тестировать без Telegram.                 |
| **Масштабируемость**           | Новый банк = обновление `bonus_calculator.py` + клавиатура. |
| **Безопасность ПДн**           | Телефон шифруется на уровне БД.                             |
| **Лёгкость поддержки**         | Каждая функция — в отдельном файле.                         |

---

## ▶️ **Как добавить новую команду (пример)**

Хотите добавить `/export_json` — экспорт данных одного реферала в JSON по номеру телефона?

1. Создайте `handlers/export_handler.py`.
2. Добавьте логику в `services/report_generator.py`.
3. Подключите роутер в `core/bot_instance.py`.

✅ Готово — без касания остального кода.

---

Такая структура идеально подходит для **вашего бота** и легко выдержит добавление любой новой функции — от новых банков до веб-панели аналитики.